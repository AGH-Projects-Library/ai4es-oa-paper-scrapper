## 2.2 Computational Domain Preparation

 This section details the workflow used to transform medical imaging data into segmentations and subsequently into a three-dimensional, multi-labeled mesh with an initial, unoptimized baffle. For each of the four patients, the end-systolic phase from the preoperative CTA was extracted and segmented into seven anatomic structures. The systolic phase was selected for segmentation and subsequent baffle construction to capture ventricular geometry during peak ejection and better reflect the anatomy relevant to outflow reconstruction. The cardiac blood pools of the left ventricle (LV), right ventricle (RV), left atrium (LA), and right atrium (RA) were segmented to define the luminal portion of the heart, including trabeculations. The myocardial wall (myo) was segmented separately to distinguish tissue from blood pool. The pulmonary artery (PA) included the main pulmonary trunk and the proximal right and left branch PAs, while the aortic root (ao) consisted of the ascending aorta with the arch and branch vessels excluded. The images were initially segmented using a U-Net–based deep learning framework. Convolutional neural networks have been well established for semantic segmentation objectives in a supervised setting with U-Net-based models remaining highly competitive [ 53 ] , especially in low-data regimes. We employed the self-configuring nnU-Net framework [ 26 ] , which automates preprocessing, training configuration, and hyperparameter selection. The model was trained on the ImageCHD dataset (67 training, 5 validation, 34 test cases) [ 68 ] and its performance was compared with a baseline U-Net implementation from SDF4CHD [ 34 ] . Training used default nnU-Net settings with DA5 data augmentation scheme, which applies extensive spatial and intensity transformations during training. The resulting segmentations, along with the corresponding CT images, were then imported into 3D Slicer for manual refinement. The first column of Figure 6 illustrates the final seven-part segmentation for all four patients, with each row corresponding to a different patient. Mislabeled regions were corrected and unlabeled voxels were assigned to the appropriate anatomical class as needed, with particular attention to the ventricular septal interface and aortic valve annulus. All segmentations were reviewed and validated by a pediatric cardiologist with subspecialty training in cardiovascular imaging (Maskatia) to ensure accuracy and methodological consistency across patients. The finalized multi-label segmentations were interpreted as three-dimensional image grids in which each voxel was assigned an integer label, ModelFaceID, corresponding to a specific anatomical structure. For each structure, a binary mask was generated by isolating voxels belonging to that label. To improve geometric and topological consistency, each binary mask underwent morphological closing and median filtering. Polygonal mesh, ℳ k \displaystyle\mathcal{M}_{k} , was extracted using the marching cubes algorithm, post-processed to remove topological artifacts (e.g., non-manifold edges), and geometrically smoothed [ 36 ] . For each subdomain mesh ℳ k \displaystyle\mathcal{M}_{k} , all triangular faces were assigned ModelFaceID = k \displaystyle k with mapping: LV =1 , RV = 2, LA = 3, RA = 4, myo = 5, ao = 6, PA = 7. For each patient, we constructed a singular, manifold mesh, M AoBiV \displaystyle M_{\text{AoBiV}} , to represent the continuous blood pool of the LV, RV, and aorta by taking the union of the three segmented regions and following the same pipeline of filtering and surface extraction. This process automatically removed internal walls between constituent structures. We used the previously generated individual meshes { ℳ k } k ∈ { 1 , 2 , 6 } \displaystyle\{\mathcal{M}_{k}\}_{k\in\{1,2,6\}} as spatial references to assign anatomical labels to ℳ AoBiV \displaystyle\mathcal{M}_{\text{AoBiV}} . ModelFaceID was assigned to each triangular face in ℳ AoBiV \displaystyle\mathcal{M}_{\text{AoBiV}} based on the nearest anatomical surface, determined from the distance between the face centroid and the corresponding labeled regions. Explicit boundary surfaces corresponding to the aortic valve annulus and the VSD were generated by truncating ℳ AoBiV \displaystyle\mathcal{M}_{\text{AoBiV}} using intersection contours between labeled anatomical subsurfaces. Specifically, the LV-RV and RV-aorta interfaces were used to define the VSD and aortic valve cutting loops, respectively. Intersection curves were computed using a vtkIntersectionPolyDataFilter. Due to mesh discretization, these raw curves were often irregular and non-planar. To regularize them, radial rays were cast from a provisional centroid onto a best-fit plane, and the first ray-surface intersection in each direction was retained. The resulting ordered surface points were smoothed to form closed, non-self-intersecting cutting loops. ℳ AoBiV \displaystyle\mathcal{M}_{\text{AoBiV}} was then truncated along each loop using a surface selection (vtkSelectPolyData) followed by clipping operation (vtkClipPolyData), and the region between the two openings was retained. The resulting open boundaries were capped with the SimVascular VTMK capping operation to produce a watertight domain. These caps were planar by construction, yielding numerically well-posed inlet and outlet boundaries that approximated the local annular regions without attempting to reproduce the full non-planar anatomy. Each cap was assigned a unique ModelFaceID corresponding to the VSD inlet and aortic outlet. The final watertight, multi-labeled domain, ℳ cappedRV \displaystyle\mathcal{M}_{\text{cappedRV}} , formed the geometric basis for subsequent baffle construction consisting of the native right ventricular wall, the VSD inlet cap, and the aortic valve outlet cap. This preprocessing pipeline to obtain ℳ cappedRV \displaystyle\mathcal{M}_{\text{cappedRV}} is summarized in Figure 2 . ![Figure 2: Illustration of the geometric preprocessing used to generate the computational domain, exemplified for patient 1. From left to right: (1) the initial patient-specific 3D anatomical model, (2) separate structures LV, RV, and myocardium, (3) a combined model ( ℳ AoBiV \displaystyle\mathcal{M}_{\text{AoBiV}} ) which consists of the LV, RV, and aorta as a continuous closed domain, and (4) the ventricular model ( ℳ cappedRV \displaystyle\mathcal{M}_{\text{cappedRV}} ) with capped faces at the VSD and aortic valve.](png/2603.25207/2603.25207/x2.png) To determine the baffle attachment boundary, a congenital cardiac surgeon (Ma) interactively defined the intended suture line on the ventricular mesh using the BafflePlanner module in the SlicerHeart extension for 3D Slicer [ 35 , 66 ] . The suture line was specified by placing 12 − 15 \displaystyle 12-15 discrete points in three-dimensional space, which delineated a clinically feasible suture path along the endocardial surface of ℳ cappedRV \displaystyle\mathcal{M}_{\text{cappedRV}} . The resulting coordinate set served as both a visual guide and as the computational input for the following automated contour generation. This sparse point set was converted into a densely sampled polyline by first projecting the points onto their two-dimensional principal component plane, where a greedy nearest-neighbor algorithm was used to establish a non-self-intersecting ordering. A parametric spline interpolant was then fit to the ordered points to generate a smooth 3D curve approximating the intended suture path. The spline was subsequently resampled at N = 600 \displaystyle N=600 equidistant parametric intervals, and each sampled point was projected back onto the surface of M cappedRV \displaystyle M_{\text{cappedRV}} using a closest-point projection. This produced the final dense, closed polyline defining the baffle boundary, ℬ baffle \displaystyle\mathcal{B}_{\text{baffle}} . ℳ cappedRV \displaystyle\mathcal{M}_{\text{cappedRV}} was partitioned along the closed curve ℬ baffle \displaystyle\mathcal{B}_{\text{baffle}} , and the region containing both the VSD inlet and aortic outlet was retained. To obtain a watertight domain for subsequent steps, this opening was capped using SimVascular, creating an initial surface that served as a temporary placeholder. The boundary of this cap precisely defines the perimeter where the final baffle will be attached. The geometry was then fully remeshed in SimVascular to provide the geometric foundation for the baffle construction detailed in the following section. The steps from surgeon selected points to the creation of a placeholder baffle are depicted in Figure 3 . ![Figure 3: Construction of placeholder baffle geometry for patient 1. From left to right: (1) ℳ cappedRV \displaystyle\mathcal{M}_{\text{cappedRV}} with a transparent myocardium for anatomical context and white circles indicating the sparse set of surgeon-selected points, (2) ℳ cappedRV \displaystyle\mathcal{M}_{\text{cappedRV}} with a transparent myocardium and densely sampled boundary curve ℬ baffle \displaystyle\mathcal{B}_{\text{baffle}} , (3) the ventricular surface after clipping along ℬ baffle \displaystyle\mathcal{B}_{\text{baffle}} , retaining the selected region, and (4) the final geometry with the cut boundary capped using SimVascular, yielding a watertight surface with a placeholder baffle surface.](png/2603.25207/2603.25207/x3.png)