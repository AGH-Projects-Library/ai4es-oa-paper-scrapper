## 3.3 Extension: Block-Sparse FlashAttention

|  | 𝐒 = 𝐐𝐊 ⊤ ∈ ℝ N × N , 𝐏 = softmax ​ ( 𝐒 ⊙ \vmathbb ​ 1 𝐌 ~ ) ∈ ℝ N × N , 𝐎 = 𝐏𝐕 ∈ ℝ N × d , formulae-sequence 𝐒 superscript 𝐐𝐊 top superscript ℝ 𝑁 𝑁 𝐏 softmax direct-product 𝐒 \vmathbb subscript 1 ~ 𝐌 superscript ℝ 𝑁 𝑁 𝐎 𝐏𝐕 superscript ℝ 𝑁 𝑑 \mathbf{S}=\mathbf{Q}\mathbf{K}^{\top}\in\mathbb{R}^{N\times N},\quad\mathbf{P}=\mathrm{softmax}(\mathbf{S}\odot\vmathbb{1}_{\tilde{\mathbf{M}}})\in\mathbb{R}^{N\times N},\quad\mathbf{O}=\mathbf{P}\mathbf{V}\in\mathbb{R}^{N\times d}, |  |
| --- | --- | --- |

 We extend FlashAttention to approximate attention: we propose block-sparse FlashAttention , whose IO complexity is smaller than FlashAttention by a factor proportional to the sparsity. Given inputs 𝐐 , 𝐊 , 𝐕 ∈ ℝ N × d 𝐐 𝐊 𝐕 superscript ℝ 𝑁 𝑑 \mathbf{Q},\mathbf{K},\mathbf{V}\in\mathbb{R}^{N\times d} and a mask matrix 𝐌 ~ ∈ { 0 , 1 } N × N ~ 𝐌 superscript 0 1 𝑁 𝑁 \tilde{\mathbf{M}}\in\{0,1\}^{N\times N} , we want to compute: [[TABLE_0]]  where ( 𝐒 ⊙ \vmathbb ​ 1 𝐌 ~ ) k ​ l = 𝐒 k ​ l subscript direct-product 𝐒 \vmathbb subscript 1 ~ 𝐌 𝑘 𝑙 subscript 𝐒 𝑘 𝑙 (\mathbf{S}\odot\vmathbb{1}_{\tilde{\mathbf{M}}})_{kl}=\mathbf{S}_{kl} if 𝐌 ~ k ​ l = 1 subscript ~ 𝐌 𝑘 𝑙 1 \tilde{\mathbf{M}}_{kl}=1 and − ∞ -\infty if 𝐌 k ​ l = 0 subscript 𝐌 𝑘 𝑙 0 \mathbf{M}_{kl}=0 . We require 𝐌 ~ ~ 𝐌 \tilde{\mathbf{M}} to have block form: for some block sizes B r , B c subscript 𝐵 𝑟 subscript 𝐵 𝑐 B_{r},B_{c} , for all k , l 𝑘 𝑙 k,l , 𝐌 ~ k , l = 𝐌 i ​ j subscript ~ 𝐌 𝑘 𝑙 subscript 𝐌 𝑖 𝑗 \tilde{\mathbf{M}}_{k,l}=\mathbf{M}_{ij} with i = ⌊ k / B r ⌋ , j = ⌊ l / B c ⌋ formulae-sequence 𝑖 𝑘 subscript 𝐵 𝑟 𝑗 𝑙 subscript 𝐵 𝑐 i=\lfloor k/B_{r}\rfloor,j=\lfloor l/B_{c}\rfloor for some 𝐌 ∈ { 0 , 1 } N / B r × N / B c 𝐌 superscript 0 1 𝑁 subscript 𝐵 𝑟 𝑁 subscript 𝐵 𝑐 \mathbf{M}\in\{0,1\}^{N/B_{r}\times N/B_{c}} . Given a predefined block sparsity mask 𝐌 ∈ { 0 , 1 } N / B r × N / B c 𝐌 superscript 0 1 𝑁 subscript 𝐵 𝑟 𝑁 subscript 𝐵 𝑐 \mathbf{M}\in\{0,1\}^{N/B_{r}\times N/B_{c}} we can easily adapt Algorithm 1 to only compute the nonzero blocks of the attention matrix. The algorithm is identical to Algorithm 1 , except we skip zero blocks. We reproduce the algorithm description in Algorithm 5 in Appendix B . We also analyze the IO complexity of block-sparse FlashAttention . Let N 𝑁 N be the sequence length, d 𝑑 d be the head dimension, and M 𝑀 M be size of SRAM with d ≤ M ≤ N ​ d 𝑑 𝑀 𝑁 𝑑 d\leq M\leq Nd . Block-sparse FlashAttention ( Algorithm 5 ) requires Θ ​ ( N ​ d + N 2 ​ d 2 ​ M − 1 ​ s ) Θ 𝑁 𝑑 superscript 𝑁 2 superscript 𝑑 2 superscript 𝑀 1 𝑠 \Theta(Nd+N^{2}d^{2}M^{-1}s) HBM accesses where s 𝑠 s is the fraction of nonzero blocks in the block-sparsity mask. We see that applying block-sparsity yields a direct improvement by the sparsity to the larger term in the IO complexity. For large sequence lengths N 𝑁 N , s 𝑠 s is often set to N − 1 / 2 superscript 𝑁 1 2 N^{-1/2} [ 11 ] or N − 1 ​ log ⁡ N superscript 𝑁 1 𝑁 N^{-1}\log N [ 92 , 3 , 17 ] , resulting in Θ ​ ( N ​ N ) Θ 𝑁 𝑁 \Theta(N\sqrt{N}) or Θ ​ ( N ​ log ⁡ N ) Θ 𝑁 𝑁 \Theta(N\log N) IO complexity. For downstream experiments, we use the fixed butterfly sparsity pattern [ 17 ] , which has been shown to be able to approximate arbitrary sparsity [ 16 ] . In Fig. 2 (right), we validate that as the sparsity increases, the runtime of block-sparse FlashAttention improves proportionally. On the LRA benchmark, block-sparse FlashAttention achieves 2.8 × \times speedup, while performing on par with standard attention ( Section 4 ).