## B.1 Memory-efficient forward pass

|  | 𝐒 = 𝐐𝐊 ⊤ ∈ ℝ N × N , 𝐏 = softmax ​ ( 𝐒 ) ∈ ℝ N × N , 𝐎 = 𝐏𝐕 ∈ ℝ N × d . formulae-sequence 𝐒 superscript 𝐐𝐊 top superscript ℝ 𝑁 𝑁 𝐏 softmax 𝐒 superscript ℝ 𝑁 𝑁 𝐎 𝐏𝐕 superscript ℝ 𝑁 𝑑 \mathbf{S}=\mathbf{Q}\mathbf{K}^{\top}\in\mathbb{R}^{N\times N},\quad\mathbf{P}=\mathrm{softmax}(\mathbf{S})\in\mathbb{R}^{N\times N},\quad\mathbf{O}=\mathbf{P}\mathbf{V}\in\mathbb{R}^{N\times d}. |  |
| --- | --- | --- |

|  | L i = ∑ j e q i T ​ k j . subscript 𝐿 𝑖 subscript 𝑗 superscript 𝑒 superscript subscript 𝑞 𝑖 𝑇 subscript 𝑘 𝑗 L_{i}=\sum_{j}e^{q_{i}^{T}k_{j}}. |  | (1) |
| --- | --- | --- | --- |

|  | o i = P i : ​ 𝐕 = ∑ j P i ​ j ​ v j = ∑ j e q i T ​ k j L i ​ v j . subscript 𝑜 𝑖 subscript 𝑃 : 𝑖 absent 𝐕 subscript 𝑗 subscript 𝑃 𝑖 𝑗 subscript 𝑣 𝑗 subscript 𝑗 superscript 𝑒 superscript subscript 𝑞 𝑖 𝑇 subscript 𝑘 𝑗 subscript 𝐿 𝑖 subscript 𝑣 𝑗 o_{i}=P_{i:}\mathbf{V}=\sum_{j}P_{ij}v_{j}=\sum_{j}\frac{e^{q_{i}^{T}k_{j}}}{L_{i}}v_{j}. |  | (2) |
| --- | --- | --- | --- |

 The main challenge in making attention memory-efficient is the softmax that couples the columns of 𝐊 𝐊 \mathbf{K} (and columns of 𝐕 𝐕 \mathbf{V} ). Our approach is to compute the softmax normalization constant separately to decouple the columns. This technique [ 60 ] has been used in the literature [ 51 , 66 ] to show that attention computation does not need quadratic extra memory (though the number of HBM accesses is still quadratic, resulting in slow run-time). For simplicity, we omit here the max-shifting step during softmax. The full algorithm in Section B.3 contains all the steps. Recall that given input sequences 𝐐 , 𝐊 , 𝐕 ∈ ℝ N × d 𝐐 𝐊 𝐕 superscript ℝ 𝑁 𝑑 \mathbf{Q},\mathbf{K},\mathbf{V}\in\mathbb{R}^{N\times d} , we want to compute the attention output 𝐎 ∈ ℝ N × d 𝐎 superscript ℝ 𝑁 𝑑 \mathbf{O}\in\mathbb{R}^{N\times d} : [[TABLE_0]]  We have that S i ​ j = q i T ​ k j subscript 𝑆 𝑖 𝑗 superscript subscript 𝑞 𝑖 𝑇 subscript 𝑘 𝑗 S_{ij}=q_{i}^{T}k_{j} where q i subscript 𝑞 𝑖 q_{i} and k j subscript 𝑘 𝑗 k_{j} are the i 𝑖 i -th and j 𝑗 j -th columns of 𝐐 𝐐 \mathbf{Q} and 𝐊 𝐊 \mathbf{K} respectively. Define the normalization constants of softmax: [[TABLE_1]]  Let v j subscript 𝑣 𝑗 v_{j} be the j 𝑗 j -th column of 𝐕 𝐕 \mathbf{V} , then the i 𝑖 i -th columns of the output is [[TABLE_2]]  We see that once L i subscript 𝐿 𝑖 L_{i} is computed, we can compute o i subscript 𝑜 𝑖 o_{i} without extra memory by repeatedly summing e q i T ​ k j L i ​ v j superscript 𝑒 superscript subscript 𝑞 𝑖 𝑇 subscript 𝑘 𝑗 subscript 𝐿 𝑖 subscript 𝑣 𝑗 \frac{e^{q_{i}^{T}k_{j}}}{L_{i}}v_{j} . Therefore the forward pass can be computed with O ​ ( n ) 𝑂 𝑛 O(n) extra memory: - 1. Compute L i subscript 𝐿 𝑖 L_{i} for all i 𝑖 i according to Eq. 1 , which takes O ​ ( n ) 𝑂 𝑛 O(n) extra memory. Compute L i subscript 𝐿 𝑖 L_{i} for all i 𝑖 i according to Eq. 1 , which takes O ​ ( n ) 𝑂 𝑛 O(n) extra memory. - 2. Compute o i subscript 𝑜 𝑖 o_{i} for all i 𝑖 i according to Eq. 2 , which takes O ​ ( d ) 𝑂 𝑑 O(d) extra memory. Compute o i subscript 𝑜 𝑖 o_{i} for all i 𝑖 i according to Eq. 2 , which takes O ​ ( d ) 𝑂 𝑑 O(d) extra memory.