## B.3 FlashAttention : Forward Pass

|  | 𝐒 = τ ​ 𝐐𝐊 ⊤ ∈ ℝ N × N , 𝐒 masked = mask ​ ( S ) ∈ ℝ N × N , 𝐏 = softmax ​ ( 𝐒 masked ) ∈ ℝ N × N , formulae-sequence 𝐒 𝜏 superscript 𝐐𝐊 top superscript ℝ 𝑁 𝑁 superscript 𝐒 masked mask 𝑆 superscript ℝ 𝑁 𝑁 𝐏 softmax superscript 𝐒 masked superscript ℝ 𝑁 𝑁 \displaystyle\mathbf{S}=\tau\mathbf{Q}\mathbf{K}^{\top}\in\mathbb{R}^{N\times N},\quad\mathbf{S}^{\mathrm{masked}}=\textsc{mask}(S)\in\mathbb{R}^{N\times N},\quad\mathbf{P}=\mathrm{softmax}(\mathbf{S}^{\mathrm{masked}})\in\mathbb{R}^{N\times N}, |  |
| --- | --- | --- |
|  | 𝐏 dropped = dropout ​ ( 𝐏 , p drop ) , 𝐎 = 𝐏 dropped ​ 𝐕 ∈ ℝ N × d , formulae-sequence superscript 𝐏 dropped dropout 𝐏 subscript 𝑝 drop 𝐎 superscript 𝐏 dropped 𝐕 superscript ℝ 𝑁 𝑑 \displaystyle\mathbf{P}^{\mathrm{dropped}}=\mathrm{dropout}(\mathbf{P},p_{\mathrm{drop}}),\quad\mathbf{O}=\mathbf{P}^{\mathrm{dropped}}\mathbf{V}\in\mathbb{R}^{N\times d}, |  |

 We describe the full details of FlashAttention forward pass. Given input sequences 𝐐 , 𝐊 , 𝐕 ∈ ℝ N × d 𝐐 𝐊 𝐕 superscript ℝ 𝑁 𝑑 \mathbf{Q},\mathbf{K},\mathbf{V}\in\mathbb{R}^{N\times d} , we want to compute the attention output 𝐎 ∈ ℝ N × d 𝐎 superscript ℝ 𝑁 𝑑 \mathbf{O}\in\mathbb{R}^{N\times d} : [[TABLE_0]]  where τ ∈ ℝ 𝜏 ℝ \tau\in\mathbb{R} is some softmax scaling (typically 1 d 1 𝑑 \frac{1}{\sqrt{d}} ), mask is some masking function that sets some entries of the input to − ∞ -\infty and keep other entries the same (e.g., key padding mask when sequences in the batch don’t have the same lengths and are padded), and dropout ​ ( x , p ) dropout 𝑥 𝑝 \mathrm{dropout}(x,p) applies dropout to x 𝑥 x elementwise (i.e., output x 1 − p 𝑥 1 𝑝 \frac{x}{1-p} with probability 1 − p 1 𝑝 1-p and output 0 with probability p 𝑝 p for each element x 𝑥 x ). The full algorithm is in Algorithm 2 . We save the output 𝐎 𝐎 \mathbf{O} , the softmax statistics ℓ ℓ \ell and m 𝑚 m , and the pseudo-random number generator state ℛ ℛ {\cal R} for the backward pass. ![Algorithm 2 FlashAttention Forward Pass]()