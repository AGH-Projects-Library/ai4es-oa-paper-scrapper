## B.4 FlashAttention : Backward Pass

 We describe the full details of FlashAttention backward pass. Given input sequences 𝐐 , 𝐊 , 𝐕 ∈ ℝ N × d 𝐐 𝐊 𝐕 superscript ℝ 𝑁 𝑑 \mathbf{Q},\mathbf{K},\mathbf{V}\in\mathbb{R}^{N\times d} , the output 𝐎 ∈ ℝ N × d 𝐎 superscript ℝ 𝑁 𝑑 \mathbf{O}\in\mathbb{R}^{N\times d} , and the output gradient 𝐝𝐎 𝐝𝐎 \mathbf{dO} , we want to compute the input gradients 𝐝𝐐 , 𝐝𝐊 , 𝐝𝐕 ∈ ℝ N × d 𝐝𝐐 𝐝𝐊 𝐝𝐕 superscript ℝ 𝑁 𝑑 \mathbf{dQ},\mathbf{dK},\mathbf{dV}\in\mathbb{R}^{N\times d} . We first describe the standard attention backward pass in Algorithm 3 for completeness. ![Algorithm 3 Standard Attention Backward Pass]() We now make two observations about FlashAttention backward pass: - 1. We do not need to store the dropout mask of size O ​ ( N 2 ) 𝑂 superscript 𝑁 2 O(N^{2}) from the forward pass. Instead, we can save the pseudo-random number generator states from the forward pass and re-generate the dropout mask in the backward pass. This allows us to only use O ​ ( N ) 𝑂 𝑁 O(N) extra memory. We do not need to store the dropout mask of size O ​ ( N 2 ) 𝑂 superscript 𝑁 2 O(N^{2}) from the forward pass. Instead, we can save the pseudo-random number generator states from the forward pass and re-generate the dropout mask in the backward pass. This allows us to only use O ​ ( N ) 𝑂 𝑁 O(N) extra memory. - 2. When computing the softmax gradient, we use Eq. 4 to compute D i = P i : ⊤ ​ d ​ P i : subscript 𝐷 𝑖 superscript subscript 𝑃 : 𝑖 absent top 𝑑 subscript 𝑃 : 𝑖 absent D_{i}=P_{i:}^{\top}dP_{i:} without reducing over P i : subscript 𝑃 : 𝑖 absent P_{i:} and d ​ P i : 𝑑 subscript 𝑃 : 𝑖 absent dP_{i:} of size N 𝑁 N (they might not fit into SRAM). Instead we can rewrite D i = d ​ o i ⊤ ​ o i subscript 𝐷 𝑖 𝑑 superscript subscript 𝑜 𝑖 top subscript 𝑜 𝑖 D_{i}=do_{i}^{\top}o_{i} and compute the dot product between vectors of size d 𝑑 d . When computing the softmax gradient, we use Eq. 4 to compute D i = P i : ⊤ ​ d ​ P i : subscript 𝐷 𝑖 superscript subscript 𝑃 : 𝑖 absent top 𝑑 subscript 𝑃 : 𝑖 absent D_{i}=P_{i:}^{\top}dP_{i:} without reducing over P i : subscript 𝑃 : 𝑖 absent P_{i:} and d ​ P i : 𝑑 subscript 𝑃 : 𝑖 absent dP_{i:} of size N 𝑁 N (they might not fit into SRAM). Instead we can rewrite D i = d ​ o i ⊤ ​ o i subscript 𝐷 𝑖 𝑑 superscript subscript 𝑜 𝑖 top subscript 𝑜 𝑖 D_{i}=do_{i}^{\top}o_{i} and compute the dot product between vectors of size d 𝑑 d . The full FlashAttention backward pass algorithm is in Algorithm 4 . Conceptually it is just a block version of the derivation in Section B.2 . ![Algorithm 4 FlashAttention Backward Pass]() We see that similar to the forward pass, the backward pass performs O ​ ( N 2 ) 𝑂 superscript 𝑁 2 O(N^{2}) FLOPs and only requires O ​ ( N ) 𝑂 𝑁 O(N) extra memory beyond inputs, output, output gradient, and input gradients. We analyze the IO-complexity of the backward pass, similar to the forward pass ( Theorem 2 ). Let N 𝑁 N be the sequence length, d 𝑑 d be the head dimension, and M 𝑀 M be size of SRAM with d ≤ M ≤ N ​ d 𝑑 𝑀 𝑁 𝑑 d\leq M\leq Nd . Standard attention ( Algorithm ) backward pass requires Θ ​ ( N ​ d + N 2 ) Θ 𝑁 𝑑 superscript 𝑁 2 \Theta(Nd+N^{2}) HBM accesses, while FlashAttention backward pass ( Algorithm 4 ) requires Θ ​ ( N 2 ​ d 2 ​ M − 1 ) Θ superscript 𝑁 2 superscript 𝑑 2 superscript 𝑀 1 \Theta(N^{2}d^{2}M^{-1}) HBM accesses. The proof is in Appendix C .