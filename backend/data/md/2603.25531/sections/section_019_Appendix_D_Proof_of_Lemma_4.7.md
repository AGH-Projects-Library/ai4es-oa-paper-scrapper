## Appendix D Proof of Lemma 4.7

 The core of this proof is that each time-bounded temporal operator β \beta in the SSTL formula contributes obligations whose lifetimes are limited to the width of its interval. Let w β = b β − a β + 1 w_{\beta}=b_{\beta}-a_{\beta}+1 be that width and let W = ∑ β w β W=\sum_{\beta}w_{\beta} (finite because the formula is finite). At any discrete position j j we track, for every active obligation, the entry index j 0 j_{0} captured when the operator became enabled. Two observations are important: - (1) Lifetime bound. An obligation created at j 0 j_{0} for operator β \beta can only stay alive while j j satisfies j 0 + a β ≤ j ≤ j 0 + b β j_{0}+a_{\beta}\leq j\leq j_{0}+b_{\beta} ; hence its lifetime is at most w β w_{\beta} steps. Lifetime bound. An obligation created at j 0 j_{0} for operator β \beta can only stay alive while j j satisfies j 0 + a β ≤ j ≤ j 0 + b β j_{0}+a_{\beta}\leq j\leq j_{0}+b_{\beta} ; hence its lifetime is at most w β w_{\beta} steps. - (2) Creation rate. Per operator, at most one new obligation can start at each position. Creation rate. Per operator, at most one new obligation can start at each position. Combining the two facts, within any sliding window of length w β w_{\beta} there can be at most w β w_{\beta} simultaneously alive obligations for β \beta . Summing over all operators yields the uniform bound W W on the number of distinct j 0 j_{0} values that must be tracked at any position. This finiteness underpins the decidability result in Lemma 4.10 .∎